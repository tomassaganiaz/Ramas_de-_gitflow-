// feature/login/login.js
function login(usuario, contraseña) {
if (usuario === "admin" && contraseña === "1234") {
    alert("Login exitoso");
} else {
    alert("Credenciales incorrectas");
}
}
